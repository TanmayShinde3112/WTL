\*\*\*	**Node.js**	 \*\*\*



1\. Runs the JS using the Google's **V8 Engine**(Same Engine as Chrome) + Node JS Library

2\. V8 Compiles JS to Native Machine code for Faster Execution.

3\. Exposes Runtime APIs (Filesystems ,Network ,timer) Not in Browser.

4\. JavaScript Runtime Environment Not Separate Framework.

5\. 



\*\*\* Express js  \*\*\*



1. Node js Frame work used to design the API and Web Application


   Query Parameter - 
   		1. It is the key value pair comping after ? in the url
   		2. It is one way to send to send the data in the backend
